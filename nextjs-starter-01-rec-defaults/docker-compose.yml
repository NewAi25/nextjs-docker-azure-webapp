version: "3.9"

services:
  build-zip:
    build:
      context: .
      dockerfile: Dockerfile.build
    volumes:
      - ./output:/output
    command: ["/bin/sh", "-c", "echo 'ZIP created in /output/deploy.zip'"]
